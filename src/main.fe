//use utils::sqrt

contract Main {
    // pub fn something(self) -> u256 {
    //     return sqrt(16)
    // }
}

#test
fn test_main(mut ctx: Context) {
    // // Create a new `Main` contract via `ctx` and test that `get_42` return 42.
    // let main: Main = Main.create(ctx, 0)
    // assert main.get_42() == 42
}